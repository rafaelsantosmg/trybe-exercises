/* Depois, faça uma pirâmide com n asteriscos de base: */

let base = 5
let string = ""

if (base % 2 !== 0) {
  for (let l = 0; l < base; l += 2) {
    for (let c = 0; c < base; c += 2) {
      if (c )
      string += "*"
      console.log(string)
    }
  }
}